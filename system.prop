# Set LCD density and checking flag
sf.lcd_density.written=0
ro.sf.lcd_density=160

# Wi-Fi
wifi.interface=wlan0

# Configure PRISM, but disable it by default
persist.tegra.didim.video=5
persist.tegra.didim.enable=0
